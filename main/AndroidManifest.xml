<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FManagement"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".homePage">
            <!-- Add any necessary intent filters or other configurations -->
        </activity>
        <activity android:name=".Monitoring">
            <!-- Add any necessary intent filters or other configurations -->
        </activity>
        <activity android:name=".MyWallet">
            <!-- Add any necessary intent filters or other configurations -->
        </activity>
        <activity android:name=".FollowWallet">
            <!-- Add any necessary intent filters or other configurations -->
        </activity>
        <activity android:name=".FinancialAdvice">
            <!-- Add any necessary intent filters or other configurations -->
        </activity>
        <activity android:name=".Advice">
            <!-- Add any necessary intent filters or other configurations -->
        </activity>
        <activity android:name=".Expense">
            <!-- Add any necessary intent filters or other configurations -->
        </activity>
        <activity android:name=".Budget">
            <!-- Add any necessary intent filters or other configurations -->
        </activity>
        <activity android:name=".DataBaseHelper">
            <!-- Add any necessary intent filters or other configurations -->
        </activity>
        <activity android:name=".SignUpActivity">
            <!-- Add any necessary intent filters or other configurations -->
        </activity>
    </application>

</manifest>